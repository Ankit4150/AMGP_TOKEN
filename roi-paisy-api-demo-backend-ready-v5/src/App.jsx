import React,{useEffect} from "react";import AppRoutes from "./routes/AppRoutes";import {useAuthStore} from "./store/authStore";import {useThemeStore} from "./store/themeStore";export default function App(){const initialize=useAuthStore(s=>s.initialize);const initTheme=useThemeStore(s=>s.initTheme);useEffect(()=>{initialize()},[initialize]);useEffect(()=>{initTheme()},[initTheme]);return <AppRoutes/>}